# 基于VLM的食物卡路里识别——夏令营考核题目

---

## 小作业：中餐食物分类（基础任务）

### 课题定位

中餐食物分类是食物卡路里识别的关键前置任务。中餐具有菜系多样、烹饪方式复杂、食材组合灵活等特点，传统基于CNN的分类方法往往难以处理开放域的中餐食物类别。本任务要求利用**视觉语言模型（VLM）**的开放词汇（open-vocabulary）能力，实现对中餐食物图像的零样本或少样本分类。

### 任务内容

#### 任务1. 中餐食物数据集构建

构建一个面向中餐食物分类的图像数据集。

要求：
- **类别覆盖**：至少包含 **20 类常见中餐食物**（如：宫保鸡丁、麻婆豆腐、红烧肉、糖醋排骨、番茄炒蛋、饺子、包子、面条、炒饭、火锅等），覆盖川菜、粤菜、鲁菜、苏菜等不同菜系
- **数据来源**：可使用公开数据集（如Food-101、ChineseFoodNet）或自行采集/网络爬取
- **标注信息**：每类至少 **30 张** 图像，包含食物类别标签
- **数据划分**：训练集 : 验证集 : 测试集 = 7 : 1 : 2
- **场景多样性**：每类食物至少覆盖 2 种场景（如：餐厅摆盘 vs. 家庭自制；正面拍摄 vs. 俯视拍摄）

#### 任务2. VLM零样本分类基线

选取至少 **1 种** VLM模型（如 CLIP、Chinese-CLIP、EVA-CLIP），实现中餐食物的零样本分类。

要求：
- 设计适配中餐食物的文本提示模板（prompt template），如 "一张{菜名}的照片"
- 实现零样本推理流程：输入图像 + 候选类别文本描述 → 输出预测类别
- 对比不同提示模板对分类性能的影响（至少对比 3 种模板）
- 计算 Top-1 和 Top-5 分类准确率

#### 任务3. 少样本学习与域适应

在零样本基础上，探索提升分类性能的方法。

要求（至少实现 **1 项**）：
- **Few-shot学习**：每类选取 5-10 张图像进行上下文学习（in-context learning），对比零样本性能提升
- **LoRA轻量微调**：在训练集上进行LoRA微调，对比零样本与微调后的性能
- **数据增强**：对训练图像进行增强（旋转、裁剪、颜色抖动等），分析对泛化性能的影响

#### 任务4. 分类结果分析与可视化

对分类结果进行系统分析。

要求：
- 绘制混淆矩阵，分析易混淆类别（如：不同炒菜类、不同面食类）
- 展示至少 **5 组** 分类失败案例，分析失败原因
- 按菜系进行分层分析，评估模型对不同菜系的识别能力差异
- 撰写简要分析报告（≥ 800 字）

### 量化指标

1. 构建包含 **≥ 20 类** 中餐食物、**≥ 600 张** 图像的数据集
2. 零样本分类 **Top-1 准确率 ≥ 45%**，**Top-5 准确率 ≥ 70%**
3. 完成至少 **1 项** 少样本/微调改进实验，性能提升 **≥ 5%**
4. 提供混淆矩阵与 **≥ 5 组** 失败案例分析
5. 输出完整的分类代码与数据预处理脚本

### 落地成果

1. 中餐食物图像数据集（含划分脚本）
2. VLM零样本分类代码（含不同prompt模板对比）
3. 少样本/微调改进实验代码
4. 分类结果分析报告（含混淆矩阵、失败案例）
5. GitHub 仓库或代码压缩包（含 README 说明）

### 推荐完成周期与难度

**难度：★★☆☆☆**

| 阶段 | 时间 | 主要任务 |
|------|------|----------|
| 阶段1 | 第1-2天 | 数据集收集、环境搭建、VLM选型 |
| 阶段2 | 第3-4天 | 零样本分类基线实现、prompt设计 |
| 阶段3 | 第5-6天 | 少样本/微调实验、性能优化 |
| 阶段4 | 第7天 | 结果分析、可视化、报告撰写 |

---

## 大作业：卡路里识别智能体（综合任务）

### 课题定位

本课题面向真实生活场景中的智能饮食健康管理需求，构建一个基于**视觉语言模型（VLM）**的**食物卡路里识别智能体**。该智能体需具备从食物图像中自动识别食物类别、估计食物分量、查询营养成分、输出卡路里信息的完整能力，并支持多轮对话式交互。

不同于单一的图像分类或回归任务，本课题强调构建一个**可解释、可交互、可迭代**的智能体系统：用户上传食物照片后，智能体不仅能输出卡路里数值，还能解释推理过程（如识别出哪些食物、估计的分量依据、可能的不确定性），并支持用户通过自然语言进行追问（如"这份饭有多少蛋白质？"、"如果我只吃了一半呢？"）。

真实场景中的核心挑战包括：
- **中餐特殊性**：中餐菜品混合度高、烹饪方式多样、酱汁覆盖导致食材可见性低
- **分量估计的不确定性**：2D图像缺乏深度信息，同一体积食物因密度差异热量差异大
- **多食物混合场景**：一盘餐食包含多种食材，需解耦并分别估计
- **用户交互的自然性**：需支持多轮对话、上下文理解、个性化调整

本课题要求申请人系统梳理VLM在食物理解领域的研究现状，设计并实现一个完整的卡路里识别智能体，验证其在真实场景中的有效性与边界。

---

### 第一部分：文献综述（理论研究）

#### 综述要求

撰写一份关于**"基于VLM的食物图像理解与营养分析"**的文献综述，字数 **≥ 3000 字**，需覆盖以下方面：

##### 1. 食物图像识别与分类（Food Image Recognition）
- 传统CNN方法在食物分类中的进展（如Food-101、VGG-Food等）
- 从封闭集分类到开放词汇识别的演进
- VLM（CLIP等）在食物开放域识别中的应用与优势

##### 2. 食物分量与体积估计（Food Portion Estimation）
- 基于深度估计的方法（如Depth-based Volume Estimation）
- 基于参考物标定的方法（如硬币、餐盘、信用卡等已知尺度物体）
- 基于多视角/三维重建的方法
- 纯2D视觉方法（如表面积-体积映射模型）

##### 3. 营养成分与卡路里估计（Nutritional Analysis）
- 基于食物类别查询营养成分表的方法
- 结合分量的卡路里计算方法
- 基于VLM的端到端卡路里估计（如Nutrition5k相关工作）

##### 4. 视觉语言模型在食物理解中的前沿应用
- VLM用于食物描述生成（如BLIP、LLaVA生成食物描述）
- VLM用于视觉问答（Food-VQA）
- 多模态大模型（GPT-4V、Gemini等）在食物场景中的零样本/少样本能力
- VLM的局限性：幻觉问题、数值推理能力弱、缺乏物理尺度感知

##### 5. 智能体（Agent）架构在健康领域的应用
- 基于LLM的健康咨询Agent
- 多模态Agent（视觉+语言）在饮食管理中的探索
- 记忆机制、工具调用（Tool Use）、多轮对话在营养分析中的作用

##### 参考文献要求
- 引用 **≥ 20 篇** 相关文献，其中近3年（2022-2025）文献占比 **≥ 50%**
- 需包含至少 3 篇顶会/顶刊论文（如CVPR、ICCV、ECCV、TPAMI、TMM等）
- 文献需分类整理，附关键论文的简要评述

---

### 第二部分：数据集构建与预处理（数据基础）

#### 数据集要求

构建或整合一个适用于卡路里识别智能体训练与评估的数据集。

##### 核心数据集（必选）

使用公开数据集作为基础，建议从以下数据集中选择：

| 数据集 | 规模 | 特点 | 适用任务 |
|--------|------|------|----------|
| **Nutrition5k** | 5K 图像 | 包含RGB+D、营养成分标注、分量信息 | 分量估计、卡路里预测 |
| **ECUSTFD** | ~2K 图像 | 中餐为主、单食物与多食物、每克卡路里标注 | 中餐场景、分量估计 |
| **Food-101** | 101K 图像 | 101类食物、无营养标注 | 预训练、类别识别 |
| **UNIMIB2016** | ~1K 图像 | 真实餐盘场景、混合食物、体积标注 | 多食物解耦、体积估计 |
| **ChineseFoodNet** | ~180K 图像 | 中国菜品、208类 | 中餐分类、预训练 |
| **Recipe1M+** | 1M+ 图文 | 菜谱-图像-营养成分关联 | 跨模态检索、营养查询 |

要求：
- 训练集 **≥ 1500 张** 带卡路里标注的图像（单食物与混合餐盘均需覆盖）
- 测试集 **≥ 300 张** 图像，需包含 **3 种场景**：
  1. **标准场景**：实验室/餐厅标准拍摄，单一食物清晰
  2. **真实场景**：手机自然拍摄，混合餐盘，复杂背景
  3. **挑战场景**：堆叠遮挡、酱汁覆盖、低光照、倾斜拍摄
- 每张图像需包含：**食物类别标签**、**卡路里值（kcal）**、**分量信息（重量或体积，如有）**
- 如使用公开数据集，需附数据预处理脚本（数据清洗、格式统一、划分策略）

##### 扩展数据集（可选加分项）
- 自行采集/标注 **≥ 100 张** 真实中餐餐盘图像
- 构建多轮对话数据：为部分图像标注用户可能的追问与标准答案（如 Q: "这份麻婆豆腐有多少蛋白质？" A: "约15克"）

---

### 第三部分：算法设计与创新（技术核心）

#### 整体架构

设计并实现一个**卡路里识别智能体（Calorie Recognition Agent）**，整体架构建议如下：

```
用户输入（图像 + 可选文本提问）
         ↓
  ┌──────┴──────┐
  ↓             ↓
图像编码器    文本编码器
 (VLM视觉)   (VLM文本/LLM)
  └──────┬──────┘
         ↓
  多模态融合与推理模块
  （食物识别 + 分量估计 + 营养查询）
         ↓
  ┌──────┴──────┬──────────────┐
  ↓             ↓              ↓
食物类别    分量/重量估计   卡路里计算
  ↓             ↓              ↓
  └──────┬──────┴──────────────┘
         ↓
  智能体输出模块
  （结构化结果 + 自然语言解释 + 对话上下文）
```

#### 核心模块要求

##### 模块1. 食物识别模块（Food Recognition Module）

基于VLM实现开放域食物识别：
- 使用 **CLIP 或 Chinese-CLIP** 进行零样本/少样本食物类别识别
- 支持 **≥ 50 类** 常见食物（中西餐均需覆盖，中餐类别 **≥ 20 类**）
- 输出：食物类别列表、置信度分数
- 对混合餐盘，需实现**多标签识别**或**检测级定位**（可用OWL-ViT、Grounding DINO等开集检测器辅助）

##### 模块2. 分量估计模块（Portion Estimation Module）

实现至少 **1 种** 分量估计方法（建议实现 **2 种** 并对比）：

**方法A：基于参考物的尺度校准**
- 检测图像中的已知尺度参考物（如标准餐盘、硬币、信用卡、手机等）
- 利用参考物像素尺寸推算食物实际面积/体积
- 结合食物密度先验（如米饭密度~0.9g/cm³、肉类密度~1.05g/cm³）估算重量

**方法B：基于深度/三维信息的分量估计**
- 如有深度图（如Nutrition5k），直接基于深度进行体积重建
- 如无深度图，利用单目深度估计模型（如MiDaS、ZoeDepth）推测相对深度，辅助体积估计

**方法C：基于VLM推理的分量估计**
- 设计Chain-of-Thought（CoT）prompt，引导VLM逐步推理分量
- 示例prompt结构："这张图片中有{食物A}和{食物B}。相对于标准餐盘，{食物A}大约占餐盘的几分之几？已知{食物A}的密度约为{X}g/cm³，估算其重量和卡路里。"

要求：
- 单食物分量估计 **MAE ≤ 30g**（或相对误差 ≤ 25%）
- 混合餐盘总体分量估计 **MAE ≤ 50g**（或相对误差 ≤ 30%）

##### 模块3. 营养查询与卡路里计算模块（Nutrition Query Module）

- 构建或使用现有的**营养成分数据库**（如USDA FoodData Central、中国食物成分表）
- 根据识别出的食物类别查询：每100g的卡路里、蛋白质、脂肪、碳水化合物含量
- 结合分量估计结果计算总卡路里及三大营养素
- 输出结构化营养信息（JSON格式），示例：
```json
{
  "foods": [
    {
      "name": "麻婆豆腐",
      "weight_g": 200,
      "calories_kcal": 220,
      "protein_g": 15,
      "fat_g": 12,
      "carbs_g": 8
    }
  ],
  "total_calories_kcal": 220
}
```

##### 模块4. 智能体交互模块（Agent Interaction Module）

构建一个支持多轮对话的交互系统：
- 使用 **LLM（如ChatGLM、Qwen、LLaMA等）** 作为对话核心
- 实现以下功能：
  1. **图像理解**：接收用户上传的食物图像，调用视觉模块分析
  2. **主动报告**：主动输出食物识别结果、分量估计、卡路里信息
  3. **问答交互**：支持用户追问（"这道菜有多少蛋白质？"、"如果只吃了一半呢？"、"这个热量高吗？"）
  4. **上下文记忆**：维护多轮对话上下文，支持指代消解（"刚才那道菜"、"左边那个"）
  5. **个性化调整**：支持用户设置目标（如减脂、增肌），给出建议
- 交互方式可选：命令行交互、Web界面（Gradio/Streamlit）、或API服务

#### 算法创新点（至少实现 **2 项**）

| 创新方向 | 描述 | 难度 |
|----------|------|------|
| **① 多食物解耦与定位** | 对混合餐盘，使用开集检测器（如Grounding DINO）定位每类食物区域，分别估计分量 | ★★★☆☆ |
| **② 不确定性量化** | 对卡路里估计输出置信区间（如"约300±50 kcal"），或基于MC Dropout/集成方法估计不确定性 | ★★★★☆ |
| **③ 少样本领域适应** | 针对用户个人饮食习惯，利用少量用户标注数据（≤20张）进行LoRA微调，提升个人场景适应性 | ★★★☆☆ |
| **④ 多模态融合优化** | 设计视觉特征与营养数据库的融合机制（如用视觉特征检索最相似的营养记录），而非简单类别查询 | ★★★★☆ |
| **⑤ 对抗域偏移** | 针对中餐与西餐的视觉差异，设计域适应策略（如对抗训练、风格迁移），提升跨菜系泛化 | ★★★★☆ |
| **⑥ Chain-of-Thought推理** | 设计结构化CoT prompt，引导VLM进行显式的分步推理（识别→分量→密度→卡路里），提升可解释性 | ★★★☆☆ |

---

### 第四部分：实验与评估（性能验证）

#### 评估指标

| 指标 | 说明 | 目标值 |
|------|------|--------|
| **食物识别 Top-1 准确率** | 单食物类别识别正确率 | ≥ 60% |
| **食物识别 Top-5 准确率** | 前5预测包含正确类别 | ≥ 85% |
| **卡路里估计 MAE（单食物）** | 平均绝对误差 | ≤ 50 kcal |
| **卡路里估计 相对误差（单食物）** | \|预测-真实\|/真实 | ≤ 20% |
| **卡路里估计 MAE（混合餐盘）** | 整盘总卡路里误差 | ≤ 100 kcal |
| **分量估计 MAE** | 重量估计平均绝对误差 | ≤ 30g |
| **交互准确率** | 用户问答中营养信息回答正确率 | ≥ 80% |

#### 实验要求

1. **基线对比实验**
   - 至少对比 **2 种VLM基线**（如 CLIP vs. LLaVA，或 CLIP vs. GPT-4V API）
   - 对比不同分量估计方法（如纯VLM估计 vs. 参考物校准）
   - 绘制性能对比表格与柱状图

2. **消融实验（Ablation Study）**
   - 验证各创新模块的有效性（如：无参考物校准 vs. 有参考物校准）
   - 验证智能体各组件的贡献（如：仅用识别模块 vs. 完整智能体）
   - 每种消融需有量化结果

3. **跨场景泛化实验**
   - 在标准场景、真实场景、挑战场景下分别测试
   - 分析不同场景下的性能下降原因

4. **用户交互测试**
   - 设计 **≥ 10 组** 典型对话测试用例（覆盖：首次询问、分量调整、营养追问、多轮上下文）
   - 记录智能体的回答与预期答案的匹配度

5. **失败案例分析**
   - 收集 **≥ 15 个** 失败案例，覆盖：识别错误、分量低估/高估、幻觉回答、上下文丢失
   - 分析每类失败的原因与改进方向

---

### 第五部分：代码与工程实现（落地要求）

#### 代码结构要求

提交完整的可运行代码，建议目录结构如下：

```
calorie-agent/
├── README.md                    # 项目说明、环境配置、运行指南
├── requirements.txt             # 依赖列表
├── config/
│   └── config.yaml              # 配置文件（模型路径、超参数等）
├── data/
│   ├── preprocess.py            # 数据预处理脚本
│   ├── nutrition_db.csv         # 营养成分数据库
│   └── README.md                # 数据集说明
├── models/
│   ├── food_recognizer.py       # 食物识别模块
│   ├── portion_estimator.py     # 分量估计模块
│   ├── nutrition_querier.py     # 营养查询模块
│   └── calorie_agent.py         # 智能体主控模块
├── tools/
│   ├── reference_detector.py    # 参考物检测工具
│   ├── depth_estimator.py       # 深度估计工具（如使用）
│   └── utils.py                 # 通用工具函数
├── experiments/
│   ├── baseline_eval.py         # 基线评估脚本
│   ├── ablation_study.py        # 消融实验脚本
│   └── visualization.py         # 结果可视化脚本
├── demo/
│   ├── cli_demo.py              # 命令行交互Demo
│   └── web_demo.py              # Web界面Demo（Gradio/Streamlit）
└── report/
    ├── literature_review.md     # 文献综述
    ├── experiment_results.md    # 实验结果与分析
    └── slides.pdf               # 答辩PPT（如有）
```

#### 代码运行要求
- 提供 `README.md`，包含：环境安装、数据准备、模型下载、运行命令
- 核心功能需可通过 **单条命令** 运行（如 `python demo/cli_demo.py --image path/to/food.jpg`）
- 代码需有适当注释，关键函数附docstring
- 提供 `requirements.txt` 或 `environment.yml`

#### 模型与资源说明
- 如模型文件较大，可提供下载脚本或网盘链接
- 标注运行所需的最低硬件配置（如GPU显存要求）
- 如使用API（如GPT-4V），需说明API Key配置方式

---

### 第六部分：总结报告

撰写一份完整的项目总结报告，结构如下：

1. **引言**（≥ 500字）
   - 研究背景与意义
   - 问题定义与挑战分析

2. **相关工作**（文献综述内容，≥ 3000字）
   - 食物图像理解研究现状
   - VLM在食物分析中的应用
   - 智能体架构进展

3. **方法**（≥ 2000字）
   - 整体架构设计
   - 各模块详细设计（识别、分量估计、营养查询、智能体交互）
   - 算法创新点详述

4. **实验**（≥ 1500字）
   - 数据集介绍
   - 实验设置与评估指标
   - 主要结果与对比分析
   - 消融实验结果
   - 失败案例分析

5. **结论与展望**（≥ 500字）
   - 工作总结
   - 局限性分析
   - 未来改进方向（如移动端部署、个性化推荐、视频分析）

**报告总字数 ≥ 8000 字**，可配合图表、流程图、截图说明。

---

### 量化指标（大作业）

| 序号 | 指标内容 | 要求 |
|------|----------|------|
| 1 | 文献综述 | ≥ 20 篇文献，≥ 3000 字，含近3年文献 ≥ 50% |
| 2 | 数据集 | 训练 ≥ 1500 张，测试 ≥ 300 张，覆盖 3 种场景 |
| 3 | 食物识别 | Top-1 准确率 ≥ 60%，Top-5 ≥ 85%，覆盖 ≥ 50 类食物 |
| 4 | 分量估计 | 实现 ≥ 1 种方法，MAE ≤ 30g 或相对误差 ≤ 25% |
| 5 | 卡路里估计 | 单食物 MAE ≤ 50 kcal 或相对误差 ≤ 20%；混合餐盘 MAE ≤ 100 kcal |
| 6 | 智能体交互 | 支持多轮对话，≥ 10 组测试用例，问答准确率 ≥ 80% |
| 7 | 算法创新 | 实现 ≥ 2 项创新点，有量化验证 |
| 8 | 消融实验 | 完成 ≥ 2 组消融实验，分析各模块贡献 |
| 9 | 失败案例 | ≥ 15 个案例分析，覆盖识别/分量/交互失败 |
| 10 | 代码工程 | 完整可运行代码，含 README，结构清晰，注释完整 |
| 11 | 总结报告 | ≥ 8000 字，结构完整，含图表 |

---

### 落地成果（大作业）

1. **卡路里识别智能体系统**（含图像分析、分量估计、营养查询、多轮对话功能）
2. **文献综述报告**（≥ 3000 字，引用 ≥ 20 篇文献）
3. **带标注的食物图像数据集**（含预处理脚本）
4. **算法创新与消融实验验证结果**
5. **完整项目代码**（GitHub仓库，含README、requirements、运行脚本）
6. **项目总结报告**（≥ 8000 字）
7. **可选**：Web交互Demo（Gradio/Streamlit）、答辩PPT

---

### 推荐完成周期与难度

**难度：★★★★☆**

| 周次 | 主要任务 |
|------|----------|
| 第1周 | 文献调研、数据集收集与整理、环境搭建、VLM与LLM选型 |
| 第2周 | 食物识别模块实现、分量估计模块实现、营养数据库构建 |
| 第3周 | 智能体交互模块实现、基线实验、消融实验、性能优化 |
| 第4周 | 跨场景测试、失败案例分析、报告撰写、代码整理与Demo制作 |

---

### 补充说明

#### 推荐技术栈

| 模块 | 推荐工具/模型 |
|------|--------------|
| VLM视觉编码 | CLIP、Chinese-CLIP、EVA-CLIP、OpenCLIP |
| VLM端到端 | LLaVA-1.5、Qwen-VL、Yi-VL、MiniGPT-4 |
| 开集检测 | Grounding DINO、OWL-ViT、YOLO-World |
| 深度估计 | MiDaS、ZoeDepth、Depth Anything |
| LLM对话 | ChatGLM3、Qwen-Chat、LLaMA2/3（中文微调版） |
| Web界面 | Gradio、Streamlit |
| 轻量微调 | PEFT（LoRA）、Hugging Face Transformers |

#### 数据集与资源链接
- Nutrition5k: https://github.com/hunterhector/nutrition5k
- ECUSTFD: https://github.com/Liuqian-666/ECUSTFD
- Food-101: https://data.vision.ee.ethz.ch/cvl/datasets_extra/food-101/
- UNIMIB2016: https://www.ivl.disco.unimib.it/activities/food-recognition/
- ChineseFoodNet: http://www.chinesefoodnet.com/
- USDA FoodData Central: https://fdc.nal.usda.gov/
- 中国食物成分表: 可查阅北京大学医学出版社《中国食物成分表》标准版

#### 计算资源建议
- **最低配置**：1块 16GB 显存 GPU（如 RTX 3080/4060 Ti）
- **推荐配置**：1块 24GB 显存 GPU（如 RTX 3090/4090 或 A5000）
- 如使用轻量模型（MobileCLIP、TinyLLaVA）或API调用，可降低至 8GB 显存
- CPU + 量化模型（INT4/INT8）可作为备用方案

#### 评分侧重

| 维度 | 权重 | 说明 |
|------|------|------|
| 文献综述 | 15% | 覆盖度、前沿性、分析深度 |
| 技术实现 | 30% | 模块完整性、代码质量、运行稳定性 |
| 算法创新 | 20% | 创新性、有效性验证、消融实验 |
| 实验结果 | 20% | 性能指标、对比分析、失败案例分析 |
| 报告与展示 | 15% | 报告质量、逻辑清晰度、工程规范 |

---

**考核提交要求**：
- 小作业：代码 + 数据集 + 简要分析报告（PDF或Markdown）
- 大作业：完整代码仓库 + 文献综述 + 总结报告 + 可选Demo视频或Web链接
- 提交方式：GitHub仓库链接（推荐）或 压缩包上传
